<p>
  <b>Content:</b>
  <%= @comment.content %>
</p>

<p>
  <b>Post:</b>
  <%= @comment.post.title %>
</p>

<p>
  <b>User:</b>
  <%= @comment.user.name %>
</p>


<%= link_to 'Edit', edit_comment_path(@comment) if signed_in? && 
	@comment.user_id.to_i == current_user.id %> | 
<%= link_to 'Back', comments_path %>
